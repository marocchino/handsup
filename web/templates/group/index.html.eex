<h1>Listing groups</h1>

<table>
  <%= for group <- @groups do %>
    <tr>
      <td><%= group.name %></td>
      <td><%= group.name_eng %></td>
      <td><%= link("edit", to: group_path(@conn, :edit, group)) %></td>
    </tr>
  <% end %>
</table>

<%= link "New Group", to: group_path(@conn, :new) %>
