<h2>New event</h2>

<%= render "form.html", changeset: @changeset,
                        action: group_event_path(@conn, :create, @group_name_eng) %>

<%= link "Back", to: event_path(@conn, :index) %>
